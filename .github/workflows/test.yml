# Testin first workflow
name: 'Testing worflow'

on : 
# Testing event inputs parameters
    workflow_dispatch:
      inputs:
       environment:
        description : test environment
        required : true
       branch-name:
        description : Branch name
        required : true
env:
  SERVICE_NAME: dazn-performance-test
jobs:
  first-job: 
    runs-on: ubuntu-latest
    name : something
    steps:
      - run: echo "Hello, GitHub Actions! ${{github.event.inputs.environment}}" 
      - run: echo "Hello, GitHub Actions! ${{github.event.inputs.branch-name}}" 
      - run: echo "Hello, GitHub Actions! ${{env.SERVICE_NAME}}" 
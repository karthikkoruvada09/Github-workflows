# Testin first workflow
name: 'Testing my first workflow file here'

on : 
    workflow_dispatch
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, GitHub Actions!" 
      - run: echo "My secret key is $TEST_KEY"